to use 
	add csv file named usernames with one column and a header of usernames
		this file contains usernames that need to be linked to ids

	add csv file name usernamesToId with two columns, one has to have header username
		this file is used to link usernames from usernames.csv to ids
it will export csv with header Username and Id

run via java sdk
